# Code Review Fix Instructions

> Generated by GitFlow CI gate for branch `feature/calendar-week-year-scheduling`  
> Overall quality score: **5.0/10**  
> **Merge is BLOCKED** until all Critical, High and Medium findings are resolved.

## Instructions for Developer Agent

1. Fix every finding listed below (Critical → High → Medium order).
2. Commit with message: `fix: address code review findings from CI gate`
3. Push to the feature branch.
4. Re-run the GitFlow merge — CI will re-run automatically.

---

## Frontend findings
**Assigned to: Developer Agent**

### 🔴 `CRITICAL` — Insecure storage of JWT tokens in localStorage

- **File**: `generated/frontend-app/src/api.ts` line 46
- **Dimension**: security
- **Detected by**: github/gpt-4o

**Issue**: Storing JWT tokens in localStorage exposes them to XSS attacks, as malicious scripts can access the tokens and potentially compromise user accounts.

**Suggested fix**: Consider using HttpOnly cookies for storing sensitive tokens. This prevents client-side scripts from accessing the tokens, reducing the risk of XSS attacks.

### 🔴 `CRITICAL` — Potential XSS vulnerability in session and unavailable period titles

- **File**: `generated/frontend-app/src/components/calendar/CalendarViews.test.tsx` line 10
- **Dimension**: security
- **Detected by**: github/gpt-4o

**Issue**: The `baseSession.title` and `baseUnavailable.reason` contain HTML tags that could lead to XSS vulnerabilities if not properly sanitized. While the tests check for sanitization, the presence of these values in test data indicates a potential security risk in the application.

**Suggested fix**: Ensure that all user-provided input is sanitized before rendering. Use a robust library like DOMPurify for sanitization and validate all user inputs on the server side.

### 🔴 `CRITICAL` — Lack of server-side validation for user inputs

- **File**: `generated/frontend-app/src/components/calendar/CalendarViews.tsx` line 1
- **Dimension**: security
- **Detected by**: github/gpt-4o

**Issue**: The code relies heavily on client-side sanitization for user inputs like session titles and unavailable period reasons. This is insufficient to prevent XSS attacks.

**Suggested fix**: Implement server-side validation and sanitization for all user inputs to ensure security against XSS and other injection attacks.

### 🟠 `HIGH` — Complex and repetitive token handling logic

- **File**: `generated/frontend-app/src/api.ts` line 87
- **Dimension**: maintainability
- **Detected by**: github/gpt-4o

**Issue**: The token handling logic in the `request` function is overly complex and contains repeated patterns for refreshing tokens and retrying requests. This increases the risk of bugs and makes the code harder to maintain.

**Suggested fix**: Refactor the token handling logic into a separate utility function or class to reduce duplication and improve readability. Consider using a library for token management if appropriate.

### 🟠 `HIGH` — Large and monolithic component

- **File**: `generated/frontend-app/src/components/CalendarPanel.tsx`
- **Dimension**: maintainability
- **Detected by**: github/gpt-4o

**Issue**: The `CalendarPanel` component is large and contains multiple responsibilities, making it difficult to test and maintain.

**Suggested fix**: Break the `CalendarPanel` component into smaller, reusable components. Each component should have a single responsibility to improve testability and maintainability.

### 🟠 `HIGH` — Hardcoded BASE_URL in API client

- **File**: `generated/frontend-app/src/api.ts` line 48
- **Dimension**: security
- **Detected by**: github/gpt-4o

**Issue**: The BASE_URL is hardcoded to 'http://127.0.0.1:8000', which is insecure and not suitable for production environments. This could lead to accidental exposure of sensitive data or misconfiguration in production.

**Suggested fix**: Use environment variables to configure the BASE_URL dynamically based on the environment (e.g., development, staging, production).

### 🟠 `HIGH` — Significant API surface area increase

- **File**: `generated/frontend-app/src/api.ts` line 609
- **Dimension**: codebase_impact
- **Detected by**: github/gpt-4o

**Issue**: The addition of calendar-related API functions (e.g., listSessions, createSession, updateSession) significantly increases the API surface area. This introduces more complexity and potential for bugs.

**Suggested fix**: Ensure that the new API functions are well-documented, tested, and follow consistent patterns with existing API functions. Consider whether all new functions are necessary or if some can be consolidated.

### 🟠 `HIGH` — Excessive component complexity in CalendarViews

- **File**: `generated/frontend-app/src/components/calendar/CalendarViews.tsx` line 1
- **Dimension**: maintainability
- **Detected by**: github/gpt-4o

**Issue**: The `MonthCalendarView`, `WeekCalendarView`, and `YearCalendarView` components are large and contain deeply nested JSX structures, making them difficult to read and maintain. This increases cognitive load for future developers.

**Suggested fix**: Break down these components into smaller, reusable subcomponents. For example, extract the rendering of individual calendar cells or rows into separate components.

### 🟠 `HIGH` — Potential XSS vulnerability in toSafeTitle function

- **File**: `generated/frontend-app/src/components/calendar/CalendarViews.tsx` line 21
- **Dimension**: security
- **Detected by**: github/gpt-4o

**Issue**: The `toSafeTitle` function relies on `sanitizeInput` for input sanitization, but the implementation of `sanitizeInput` is not provided in the diff. If `sanitizeInput` is not robust, this could lead to XSS vulnerabilities.

**Suggested fix**: Ensure that `sanitizeInput` is implemented securely, using a well-tested library like DOMPurify to sanitize user input. Additionally, validate and sanitize inputs on the server side.

### 🟡 `MEDIUM` — Inconsistent token storage approach

- **File**: `generated/frontend-app/src/api.ts` line 114
- **Dimension**: coding_standards
- **Detected by**: github/gpt-4o

**Issue**: The refresh token is stored in `localStorage`, which is a poor practice as it exposes the token to potential XSS attacks. This is inconsistent with modern security practices.

**Suggested fix**: Use HTTP-only, secure cookies to store sensitive tokens like refresh tokens. This prevents JavaScript from accessing the token and reduces the risk of XSS attacks.

### 🟡 `MEDIUM` — Introduction of new dependencies

- **File**: `generated/frontend-app/package-lock.json`
- **Dimension**: codebase_impact
- **Detected by**: github/gpt-4o

**Issue**: Several new dependencies have been added to `package-lock.json`, including testing libraries and CSS tools. While these may be necessary, they increase the size and complexity of the project and may introduce new vulnerabilities.

**Suggested fix**: Evaluate the necessity of each new dependency and ensure they are actively maintained and free of known vulnerabilities. Consider using tools like `npm audit` to check for security issues.

### 🟡 `MEDIUM` — Missing test coverage for new dependencies

- **File**: `generated/frontend-app/package.json` line 8
- **Dimension**: technical_debt
- **Detected by**: github/gpt-4o

**Issue**: New testing-related dependencies (e.g., `@testing-library/react`, `vitest`) have been added, but there is no indication that tests have been written or updated to utilize these tools.

**Suggested fix**: Ensure that new tests are written or existing tests are updated to leverage the added testing libraries. Include test coverage reports to verify the effectiveness of the tests.

### 🟡 `MEDIUM` — Node version compatibility issues

- **File**: `generated/frontend-app/package-lock.json`
- **Dimension**: codebase_impact
- **Detected by**: github/gpt-4o

**Issue**: Several new dependencies (e.g., `@csstools/css-tokenizer`, `@testing-library/dom`) require Node.js version 18 or higher. This could cause compatibility issues if the project is running on an older Node.js version.

**Suggested fix**: Verify the Node.js version used in the project and update it to meet the requirements of the new dependencies. Update documentation to reflect the new Node.js version requirement.

### 🟡 `MEDIUM` — Potential security risks from new dependencies

- **File**: `generated/frontend-app/package-lock.json`
- **Dimension**: security
- **Detected by**: github/gpt-4o

**Issue**: New dependencies, such as `@testing-library/jest-dom` and `@csstools/css-tools`, introduce additional attack surfaces. These dependencies may have vulnerabilities that could compromise the application.

**Suggested fix**: Run `npm audit` to identify and address any vulnerabilities in the new dependencies. Regularly monitor these dependencies for updates and security patches.

### 🟡 `MEDIUM` — Inconsistent import ordering

- **File**: `generated/frontend-app/src/App.tsx` line 1
- **Dimension**: coding_standards
- **Detected by**: github/gpt-4o

**Issue**: The import statements are not consistently ordered. For example, external dependencies like 'react' should be grouped and placed before internal imports. This improves readability and maintainability.

**Suggested fix**: Group imports into categories (e.g., external libraries, internal components, utilities) and order them alphabetically within each group.

### 🟡 `MEDIUM` — Event listener management in useEffect

- **File**: `generated/frontend-app/src/App.tsx` line 68
- **Dimension**: maintainability
- **Detected by**: github/gpt-4o

**Issue**: The 'auth:expired' event listener is added and removed in a useEffect hook, but there is no check to ensure that the event listener is not added multiple times, which could lead to memory leaks or unexpected behavior.

**Suggested fix**: Ensure that the event listener is added only once by using a ref or a similar mechanism to track whether it has already been added.

### 🟡 `MEDIUM` — Insufficient test coverage for CalendarPanel

- **File**: `generated/frontend-app/src/components/CalendarPanel.test.tsx`
- **Dimension**: technical_debt
- **Detected by**: github/gpt-4o

**Issue**: The test file for CalendarPanel only includes a single test that checks if the module loads without errors. This does not provide sufficient coverage for the component's functionality.

**Suggested fix**: Add tests for key functionalities of the CalendarPanel component, such as rendering calendar views, handling user interactions, and API calls.

### 🟡 `MEDIUM` — Inline state initialization

- **File**: `generated/frontend-app/src/components/CalendarPanel.tsx` line 1
- **Dimension**: technical_debt
- **Detected by**: github/gpt-4o

**Issue**: State initialization for 'monthCursor', 'weekStartHour', and 'weekEndHour' is done inline using functions. While functional, this approach can make the component harder to read and test.

**Suggested fix**: Consider extracting the state initialization logic into separate utility functions or a custom hook to improve readability and reusability.

### 🟡 `MEDIUM` — Potential token expiration race condition

- **File**: `generated/frontend-app/src/api.ts` line 128
- **Dimension**: security
- **Detected by**: github/gpt-4o

**Issue**: The ensureValidAccessToken function clears the token and throws an error if the token is expired or near expiry. However, there is no mechanism to refresh the token, which could lead to user sessions being prematurely terminated.

**Suggested fix**: Implement a token refresh mechanism to obtain a new token before the current one expires. This will improve the user experience and prevent unnecessary logouts.

### 🟡 `MEDIUM` — Complex state management in CalendarPanel

- **File**: `generated/frontend-app/src/components/CalendarPanel.tsx` line 1
- **Dimension**: maintainability
- **Detected by**: github/gpt-4o

**Issue**: The CalendarPanel component manages a large number of state variables, making it difficult to follow the logic and increasing the risk of bugs.

**Suggested fix**: Refactor the component to use a state management library (e.g., Redux, Zustand) or custom hooks to encapsulate related state and logic.

### 🟡 `MEDIUM` — New Calendar module increases application complexity

- **File**: `generated/frontend-app/src/App.tsx` line 224
- **Dimension**: codebase_impact
- **Detected by**: github/gpt-4o

**Issue**: The addition of the Calendar module introduces new dependencies, state management, and UI components. This increases the overall complexity of the application.

**Suggested fix**: Ensure that the Calendar module is well-documented and modular. Consider lazy-loading the module to reduce the initial load time of the application.

### 🟡 `MEDIUM` — Inconsistent naming for authentication token functions

- **File**: `generated/frontend-app/src/components/LoginScreen.tsx` line 6
- **Dimension**: coding_standards
- **Detected by**: github/gpt-4o

**Issue**: The function `setAuthTokens` replaces `setToken` and `clearToken`, but the naming is inconsistent with the rest of the API functions like `setCurrentUsername`. The pluralization of 'Tokens' is unnecessary and inconsistent.

**Suggested fix**: Rename `setAuthTokens` to `setToken` to maintain consistency with the naming convention used in the rest of the API functions.

### 🟡 `MEDIUM` — Hardcoded strings in JSX

- **File**: `generated/frontend-app/src/components/calendar/CalendarViews.tsx` line 1
- **Dimension**: technical_debt
- **Detected by**: github/gpt-4o

**Issue**: Several hardcoded strings, such as 'Edit', 'Unavailable', and 'session', are used directly in JSX. This makes localization and future updates more difficult.

**Suggested fix**: Extract all hardcoded strings into a centralized constants or localization file, similar to `messages.ts`, to improve maintainability and support for internationalization.

### 🟡 `MEDIUM` — Excessive use of inline functions in JSX

- **File**: `generated/frontend-app/src/components/calendar/CalendarViews.tsx` line 1
- **Dimension**: maintainability
- **Detected by**: github/gpt-4o

**Issue**: Inline functions are used extensively in JSX, such as in the `onClick` handlers for buttons. This can lead to performance issues as new function instances are created on every render.

**Suggested fix**: Move inline functions to class methods or use `useCallback` hooks to memoize these functions and improve performance.

### 🟡 `MEDIUM` — Breaking change in API function signature

- **File**: `generated/frontend-app/src/components/LoginScreen.tsx` line 1
- **Dimension**: codebase_impact
- **Detected by**: github/gpt-4o

**Issue**: The `setToken` and `clearToken` functions have been replaced by `setAuthTokens`. This is a breaking change that could affect other parts of the codebase relying on the old functions.

**Suggested fix**: Ensure that all references to `setToken` and `clearToken` are updated throughout the codebase. Consider providing a migration guide or deprecating the old functions with warnings before removal.

### 🟡 `MEDIUM` — Code duplication in calendar components

- **File**: `generated/frontend-app/src/components/calendar/CalendarViews.tsx` line 1
- **Dimension**: technical_debt
- **Detected by**: github/gpt-4o

**Issue**: The `MonthCalendarView` and `WeekCalendarView` components share similar logic for rendering calendar cells and events, leading to code duplication.

**Suggested fix**: Refactor the shared logic into reusable utility functions or components to reduce duplication and improve maintainability.

---

## Backend findings
**Assigned to: Developer Agent**

### 🔴 `CRITICAL` — Insufficient authorization checks in `SessionsListView`

- **File**: `generated/backend-app/api/sessions_views.py` line 52
- **Dimension**: security
- **Detected by**: github/gpt-4o

**Issue**: The `SessionsListView` and `SessionsDetailView` rely on `OwnsObjectPermission` for authorization. However, the `OwnsObjectPermission` implementation does not account for cases where the `owner` or `coach` attributes are missing or manipulated, potentially allowing unauthorized access.

**Suggested fix**: Enhance the `OwnsObjectPermission` class to include stricter checks and ensure that all objects have the expected attributes. Additionally, add unit tests to verify the behavior of the permission class.

### 🔴 `CRITICAL` — Missing validation for user input in SessionsListView

- **File**: `api/sessions_views.py` line 32
- **Dimension**: security
- **Detected by**: github/gpt-4o

**Issue**: The `SessionsListView` does not validate user input for the `page_size` query parameter, which could lead to performance issues or denial-of-service attacks if a very large value is provided.

**Suggested fix**: Enforce a maximum limit on the `page_size` query parameter to prevent abuse. This can be done by setting a default and maximum value in the `CalendarPageNumberPagination` class.

### 🟠 `HIGH` — Breaking change in `Session` model

- **File**: `generated/backend-app/api/models.py` line 91
- **Dimension**: codebase_impact
- **Detected by**: github/gpt-4o

**Issue**: The `Session` model has been modified to include new fields (`duration_minutes`, `coachee`, `notes`) and changes to the `coachee` field's `related_name`. These changes may break existing code or APIs that rely on the previous model structure.

**Suggested fix**: Ensure that all dependent code and APIs are updated to handle the new fields and `related_name`. Consider providing a migration guide or backward compatibility if this change affects external clients.

### 🟠 `HIGH` — Potential security issue with `OwnsObjectPermission`

- **File**: `generated/backend-app/api/sessions_views.py` line 10
- **Dimension**: security
- **Detected by**: github/gpt-4o

**Issue**: The `OwnsObjectPermission` class assumes that objects will always have either an `owner` or `coach` attribute. If an object lacks these attributes, the method will return `False` by default, which may lead to unintended behavior or security vulnerabilities.

**Suggested fix**: Add explicit error handling or logging for cases where neither `owner` nor `coach` attributes are present. Consider raising an exception if the object does not have the expected attributes.

### 🟠 `HIGH` — Expanded API surface area

- **File**: `generated/backend-app/api/sessions_views.py` line 1
- **Dimension**: codebase_impact
- **Detected by**: github/gpt-4o

**Issue**: The addition of new views for `WeeklyAvailabilityWindow` and `UnavailablePeriod` significantly expands the API surface area. This increases the potential for bugs and security vulnerabilities.

**Suggested fix**: Ensure that the new API endpoints are thoroughly tested and documented. Consider conducting a security review to identify potential vulnerabilities.

### 🟠 `HIGH` — Potential privilege escalation in OwnsObjectPermission

- **File**: `api/permissions.py` line 20
- **Dimension**: security
- **Detected by**: github/gpt-4o

**Issue**: The `OwnsObjectPermission` class relies on the presence of `owner_id` or `coach_id` attributes to determine ownership. If an object lacks these attributes, the permission check defaults to `False`, but this behavior is not explicitly enforced. This could lead to potential privilege escalation if an object with unexpected attributes is passed.

**Suggested fix**: Explicitly raise an exception or log an error when neither `owner_id` nor `coach_id` is present, instead of silently returning `False`.

### 🟠 `HIGH` — Introduction of new API endpoints

- **File**: `api/sessions_views.py` line 7
- **Dimension**: codebase_impact
- **Detected by**: github/gpt-4o

**Issue**: The addition of new views for `WeeklyAvailabilityWindow` and `UnavailablePeriod` significantly expands the API surface. This introduces new potential points of failure and security risks.

**Suggested fix**: Ensure that these new endpoints are thoroughly documented and tested. Conduct a security review to verify that they do not expose sensitive data or allow unauthorized access.

### 🟠 `HIGH` — Potential lack of authorization checks for sensitive endpoints

- **File**: `generated/backend-app/coaching_backend/urls.py` line 41
- **Dimension**: security
- **Detected by**: github/gpt-4o

**Issue**: The new endpoints for sessions and availability (e.g., `/api/sessions/`, `/api/availability/windows/`) do not indicate any explicit authorization checks. This could allow unauthorized users to access or modify sensitive data.

**Suggested fix**: Ensure that all endpoints are protected with appropriate authentication and authorization mechanisms, such as decorators or middleware, to prevent unauthorized access.

### 🟡 `MEDIUM` — No-op migration with unclear purpose

- **File**: `generated/backend-app/api/migrations/0002_message_task_id.py`
- **Dimension**: technical_debt
- **Detected by**: github/gpt-4o

**Issue**: The migration file is a no-op with a comment explaining that it is to preserve historical ordering. However, this can lead to confusion for developers and may indicate a lack of proper migration management.

**Suggested fix**: Consider consolidating or removing unnecessary migrations to reduce clutter and improve clarity. If the no-op migration is required, provide a more detailed explanation in the comment.

### 🟡 `MEDIUM` — Ambiguous field name 'notes'

- **File**: `api/models.py` line 83
- **Dimension**: coding_standards
- **Detected by**: github/gpt-4o

**Issue**: The field `notes` in the `Session` model is too generic and does not provide enough context about its purpose. It could lead to confusion when the codebase grows.

**Suggested fix**: Rename the field to something more descriptive, such as `session_notes` or `coachee_notes`, to improve clarity.

### 🟡 `MEDIUM` — Unclear purpose of _resolve_actor function

- **File**: `api/sessions_serializers.py` line 5
- **Dimension**: maintainability
- **Detected by**: github/gpt-4o

**Issue**: The `_resolve_actor` function is overly complex and its purpose is not immediately clear. It also raises a `ValidationError` for what seems to be a logic issue, which is not ideal.

**Suggested fix**: Refactor `_resolve_actor` to simplify its logic and ensure it only raises exceptions for validation errors. Consider renaming the function to better reflect its purpose, such as `get_authenticated_user_or_owner`.

### 🟡 `MEDIUM` — Code duplication in CoachOwnedListCreateView and CoachOwnedDetailView

- **File**: `api/sessions_views.py` line 12
- **Dimension**: technical_debt
- **Detected by**: github/gpt-4o

**Issue**: The `get_queryset` and `perform_create` methods in `CoachOwnedListCreateView` and `CoachOwnedDetailView` are nearly identical, leading to code duplication.

**Suggested fix**: Extract the shared logic into a mixin class that can be reused across both views to reduce duplication and improve maintainability.

### 🟡 `MEDIUM` — Insufficient test coverage for edge cases

- **File**: `api/tests/test_permissions.py`
- **Dimension**: technical_debt
- **Detected by**: github/gpt-4o

**Issue**: The test cases for `OwnsObjectPermission` do not cover scenarios where the `coach_id` or `owner_id` attributes are present but contain invalid values.

**Suggested fix**: Add test cases to cover edge cases, such as when `owner_id` or `coach_id` are set to invalid or unexpected values, to ensure the permission logic is robust.

### 🟡 `MEDIUM` — Complex validation logic in serializers

- **File**: `api/sessions_serializers.py` line 80
- **Dimension**: maintainability
- **Detected by**: github/gpt-4o

**Issue**: The validation logic in `WeeklyAvailabilityWindowSerializer` and `UnavailablePeriodSerializer` is complex and could be difficult to maintain or extend.

**Suggested fix**: Consider extracting the validation logic into separate utility functions or classes to improve readability and testability.

### 🟡 `MEDIUM` — Excessive import statements in a single line

- **File**: `generated/backend-app/coaching_backend/urls.py` line 8
- **Dimension**: coding_standards
- **Detected by**: github/gpt-4o

**Issue**: The import statement for `api.plans_views` includes multiple classes in a single line, which reduces readability and makes it harder to track changes or identify unused imports.

**Suggested fix**: Split the import statement into multiple lines, with one class per line, to improve readability and maintainability.

### 🟡 `MEDIUM` — Lack of URL namespace for new endpoints

- **File**: `generated/backend-app/coaching_backend/urls.py` line 41
- **Dimension**: maintainability
- **Detected by**: github/gpt-4o

**Issue**: The new endpoints for sessions and availability are added directly to the root `urlpatterns` without a namespace. This can lead to potential conflicts and makes it harder to manage URLs in larger projects.

**Suggested fix**: Group the new endpoints under a namespace (e.g., `sessions/` or `availability/`) to improve organization and avoid potential conflicts.

---

## General / Agent findings
**Assigned to: Developer Agent**

### 🟡 `MEDIUM` — Incomplete API documentation

- **File**: `docs/calendar-api.md`
- **Dimension**: technical_debt
- **Detected by**: github/gpt-4o

**Issue**: The API documentation does not include examples of error responses for all endpoints, nor does it specify the expected behavior for edge cases like invalid input or unauthorized access.

**Suggested fix**: Expand the API documentation to include detailed examples of error responses and edge case handling for all endpoints. This will improve developer experience and reduce the likelihood of misuse.
